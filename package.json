{
  "name": "online-store",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "start:server": "json-server --watch ./server/db.json --routes ./server/routes.json --port 3001 --",
    "start:client": "parcel index.html",
    "build": "parcel build index.html --dist-dir build",
    "start": "concurrently --kill-others 'npm run start:client' 'npm run start:server'",
    "clean": "rimraf output/dist",
    "test": "jest"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@babel/core": "^7.23.5",
    "@babel/preset-env": "^7.23.5",
    "@testing-library/jest-dom": "^6.1.4",
    "babel-jest": "^29.7.0",
    "concurrently": "^8.2.2",
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^29.7.0",
    "jest-fetch-mock": "^3.0.3",
    "json-server": "^0.17.4",
    "parcel": "^2.3.2",
    "pretty-format": "^29.7.0",
    "rimraf": "^3.0.2"
  }
}
