{
  "name": "solution",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "start:backend": "json-server --watch ./server/db.json --routes ./server/routes.json --port 3001 --",
    "build": "parcel build index.html --dist-dir output/dist",
    "start": "parcel index.html",
    "clean": "rimraf output/dist"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "json-server": "^0.17.4",
    "parcel": "^2.3.2",
    "rimraf": "^3.0.2"
  }
}
